<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.plantify">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plantify">
        <activity
            android:name=".info_screen.SignupScreen"
            android:exported="false" />
        <activity
            android:name=".info_screen.LoginScreen"
            android:exported="false" />
        <activity
            android:name=".info_screen.SecondScreen"
            android:exported="false" />
        <activity
            android:name=".info_screen.FirstScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>